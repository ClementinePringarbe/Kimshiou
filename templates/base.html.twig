<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="stylesheet" href="{{ asset('css/kimshiou.css') }}">

        <title>Kimshiou</title>
        <link rel="icon" href="{{ asset('icons8-kimchi2.png') }}">


        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>

    {# La div container-fluid contient toute la navigation. #}
    <div class="container-fluid">
    {# Navigation #}
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        
            {# Ici nous avons le titre du site, qui redirige sur l'accueil ainsi que l'icône. #}
                <a href="{{ path('home') }}" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none header-logo">
                    <img src="{{asset('icons8-kimchi2.png')}}" height="24" alt="Logo Kimshiou">
                    <span class="header-logo__title">Kimshiou</span>
                </a>
                
                {# Une autre div contient une liste non ordonnée qui contient elle même les liens principaux de la navigation. #}
                {# <div class="collapse navbar-collapse" id="navbarSupportedContent"> #}
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path('home') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('product') }}">Produits</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('recipe') }}">Recettes</a>
                        </li>
                    </ul>
                    {# Une autre liste non ordonnée contient les liens vers le backoffice. #}
                            <ul class="dropdown-menu">
                                <li class="nav-item"><a class="dropdown-item" href="#">Profil</a></li>
                                <li class="nav-item"><a class="dropdown-item" href="#">Backoffice</a></li>
                                <li class="nav-item"><a class="dropdown-item" href="#">Déconnexion</a></li>
                            </ul>

                    <button type="button" class="btn btn-danger me-2">Connexion</button>

                    <form class="d-flex" action="list.html">
                        <input class="form-control form-control-sm me-2" name="search" type="search" placeholder="Rechercher...">
                        <button class="btn btn-outline-danger" type="submit"><i class="bi bi-search"></i></button>
                    </form>
                {# </div> #}
            </div>
        </nav>

        {% block body %}{% endblock %}

    {# Footer #}
    <footer class="container-fluid py-4 border-top text-center">
            <span class="text-muted">Kimshiou fait avec &hearts; par Kimshiou !
            </span>
            <ul>
            <li class="nav-item"><a class="links-footer" href="#">Mentions légales</a></li>
            <li class="nav-item"><a class="links-footer" href="#">A propos</a></li>
            <li class="nav-item"><a class="links-footer" href="#">Contact</a></li>
            </ul>
        </footer>

    </body>
</html>





